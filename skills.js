function calculateNumbers(var1, var2) {
    return var1 + var2; 
}

function displayResult(result) {
    console.log(result);
}

// Calling the functions with some example values
displayResult(calculateNumbers(5, 10)); // Output: 15   (5 + 10 = 15)


